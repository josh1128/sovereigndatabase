name: Remove undefined map title

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/remove-undefined-map-title.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python .github/scripts/remove_undefined_map_title.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add dashboard.py
          git commit -m "Remove undefined map title"
          git push
